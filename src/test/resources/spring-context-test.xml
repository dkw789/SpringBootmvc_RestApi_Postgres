<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:util="http://www.springframework.org/schema/util"
	 xmlns:tx="http://www.springframework.org/schema/tx"

	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd
        http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.1.xsd">

	<bean id="propertyPlaceholderConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
			<list>
				<value>classpath:unitils.properties</value>
			</list>
		</property>
		<property name="ignoreResourceNotFound" value="false" />
		<property name="ignoreUnresolvablePlaceholders" value="false" />
	</bean>

	<context:annotation-config />
<!-- 	<context:component-scan base-package="messaging.*" /> -->

 <context:annotation-config />
 <context:component-scan base-package="messaging" >
 <!--<context:exclude-filter type="regex" expression="messaging.*config.*"/>-->
 </context:component-scan>


<bean id="resttemplate"
	class="org.springframework.web.client.RestTemplate">
	</bean>
	
	
        <bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">

		<property name="driverClassName" value="${database.driverClassName}" />
		<property name="url" value="${database.url}" />
		<property name="username" value="${database.userName}" />
		<property name="password" value="${database.password}" />
	</bean>
 
	<bean id="entityManagerFactory"
		    class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean"
		p:persistenceUnitName="service-jpa-pu"
        p:dataSource-ref="dataSource"
        p:jpaVendorAdapter-ref ="hibernateVendorAdapter"
		p:jpaPropertyMap-ref="jpaPropertyMap" 
		p:packagesToScan="messaging"
		>
	<!-- 	 <property name="persistenceXmlLocation" value="classpath:META-INF/persistence.xml" />  -->
		 </bean>

	<util:map id="jpaPropertyMap">
		<entry key="hibernate.hbm2ddl.auto" value="${hibernate.ddl}" />
		<entry key="hibernate.default_schema" value="${hibernate.schema}" />
	</util:map>

	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager"
		p:entityManagerFactory-ref="entityManagerFactory" />

<!-- 	<bean id="vendorAdapter" class="org.unitils.orm.jpa.util.provider.hibernate.UnitilsHibernateJpaVendorAdapter"
		p:showSql="${hibernate.show.sql}" p:database="${database.vendor}">
	</bean>
	 -->
 <bean id="hibernateVendorAdapter" class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"
		p:showSql="${hibernate.show.sql}" p:database="${database.vendor}">
	</bean>

</beans>